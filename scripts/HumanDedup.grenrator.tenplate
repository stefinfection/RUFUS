#!/bin/bash

CDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

RDIR=$CDIR
BAMTOOLS=/uufs/chpc.utah.edu/common/HIPAA/u0746015/bin/container_versions/bamtools/build/src/bamtools
$BAMTOOLS filter -in $1 -isDuplicate false | $BAMTOOLS convert -format fastq 
